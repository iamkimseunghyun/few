{
  "installCommand": "npm install --force && npm rebuild",
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "regions": ["icn1"],
  "env": {
    "NODE_ENV": "production",
    "SKIP_ENV_VALIDATION": "true"
  }
}